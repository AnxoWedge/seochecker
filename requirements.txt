# seochecker — Phase 1 dependencies
httpx[http2]==0.28.1     # async client, HTTP/2, streaming
selectolax==0.4.11       # fast HTML parsing (Modest/Lexbor)
brotli==1.2.0            # so we can honestly advertise `br` in Accept-Encoding
zstandard==0.25.0        # ...and `zstd`
jinja2==3.1.6           # HTML report templating
flask==3.1.3             # the dashboard (serve.py); the CLI works without it
rich==15.0.0             # CLI output

# Optional: JavaScript rendering (--render auto/always).
# Everything above works without it; `--render never` skips it entirely.
#   ./venv/bin/pip install playwright==1.62.0
#   ./venv/bin/python -m playwright install chromium
